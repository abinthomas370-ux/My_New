name: test2
env:
  APP_NAME: devops-company-site
  ENVIRONMENT: development
  COMPANY: CloudNova
 
on:
  push:
    branches:
      -main
      -abinthomas370-ux-patch-2
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    -  name: repository test
       uses: actions/checkout@v4
    -  name: list
       run: | 
         echo $APP_NAME 
         echo $ENVIRONMENT
         echo $COMPANY
       
    -  name: date
       run: date
