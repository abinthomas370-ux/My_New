name: Website CI
on:
  push:
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   -  name: Checkout
      uses: actions/checkout@v4
   -  name: list
      run: ls
 verify: 
   needs: build 
   runs-on: ubuntu-latest
   steps:
    -  name: Finsih 
       run: echo Finished
 
